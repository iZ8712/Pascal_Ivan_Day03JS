function box (width, height, depth) {
        
        var area = width * height;
        var volum = width * height * depth;
                
        document.write("The area of the box is: " + area + "<br> The volume of the box is: " + volum);
    
}

box(2,7,5);